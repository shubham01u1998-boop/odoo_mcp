# Function Index

_Auto-generated by `scripts/gen_index.py` — do not edit manually._

| Function | File | Signature | Description |
|---|---|---|---|
| `get_ticket` | `tools/read.py` | `get_ticket(ticket_id: int, detail: bool = False, fresh: bool = False, model: str = 'project.task') → dict` | Fetch a single task by ID. Set detail=true for description, subtask count, and dates. Set fresh=true to bypass graph and re-fetch from Odoo. |
| `list_tickets` | `tools/read.py` | `list_tickets(project_id: int | None = None, stage: str | None = None, tag: str | None = None, assigned_to: int | None = None, priority: str | None = None, search: str | None = None, limit: int = 20, offset: int = 0, fresh: bool = False, model: str = 'project.task') → dict` | List tasks with optional filters. Returns paginated identity envelopes (max 50 per call). |
| `get_ticket_summary` | `tools/read.py` | `get_ticket_summary(ticket_ids: list[int], model: str = 'project.task') → list[dict]` | One-liner summary per ticket. Most token-efficient overview. Max 100 IDs. |
| `search_tickets` | `tools/read.py` | `search_tickets(query: str, project_id: int | None = None, limit: int = 10, fresh: bool = False, model: str = 'project.task') → dict` | Full-text search across task title and description. |
| `list_attachments` | `tools/read.py` | `list_attachments(ticket_id: int, model: str = 'project.task') → list[dict]` | List all files attached to a ticket. Returns [{id, filename, mimetype, size, created_at}]. |
| `get_attachment` | `tools/read.py` | `get_attachment(attachment_id: int) → dict` | Fetch the decoded text content of an attachment by ID. Use list_attachments first to find the ID. |
| `create_project` | `tools/write.py` | `create_project(name: str, description: str | None = None, stages: list[str] | None = None) → dict` | Create a new Odoo project. Auto-creates stages if not supplied. Returns project + stage IDs. |
| `create_stage` | `tools/write.py` | `create_stage(name: str, project_id: int, sequence: int = 10) → dict` | Create a Kanban stage/sprint column and assign it to a project. |
| `create_ticket` | `tools/write.py` | `create_ticket(title: str, project_id: int, description: str | None = None, stage_id: int | None = None, assignee_ids: list[int] | None = None, tag_ids: list[int] | None = None, priority: str = '0', deadline: str | None = None, subtasks: list[str] | None = None, model: str = 'project.task') → dict` | Create a new task. Returns {id, title, stage, description}. |
| `bulk_create_stages` | `tools/write.py` | `bulk_create_stages(stages: list[dict], project_id: int) → dict` | Create multiple stages for a project. Each stage: {name, sequence?}. |
| `create_tag` | `tools/write.py` | `create_tag(name: str) → dict` | Create a new project tag. Returns {id, name}. |
| `add_subtasks` | `tools/write.py` | `add_subtasks(ticket_id: int, subtasks: list[str], model: str = 'project.task') → dict` | Add subtasks (child tasks) to an existing ticket. Returns {ticket_id, created, subtask_ids}. |
| `bulk_create_tickets` | `tools/write.py` | `bulk_create_tickets(tickets: list[dict], project_id: int) → dict` | Create multiple tickets. Each ticket: {title, stage_id?, description?, assignee_ids?, tag_ids?, priority?, deadline?, subtasks?}. |
| `update_ticket` | `tools/write.py` | `update_ticket(ticket_id: int, title: str | None = None, description: str | None = None, stage_id: int | None = None, assignee_ids: list[int] | None = None, priority: str | None = None, deadline: str | None = None, model: str = 'project.task') → dict` | Patch-update a task. Only fields explicitly passed are changed in Odoo. |
| `delete_ticket` | `tools/write.py` | `delete_ticket(ticket_id: int, model: str = 'project.task') → dict` | Permanently delete a task by ID. Returns {ticket_id, deleted}. |
| `add_comment` | `tools/write.py` | `add_comment(ticket_id: int, body: str, model: str = 'project.task') → dict` | Post a public chatter message on a ticket. Notifies followers. Returns {ticket_id, message_id}. |
| `post_log_note` | `tools/write.py` | `post_log_note(ticket_id: int, body: str, model: str = 'project.task') → dict` | Post an internal log note on a ticket. Visible to internal users only — does NOT notify followers. Returns {ticket_id, message_id}. |
| `attach_file` | `tools/write.py` | `attach_file(ticket_id: int, filename: str, content: str, mimetype: str | None = None, overwrite: bool = False, model: str = 'project.task') → dict` | Attach a file to a ticket. content must be a UTF-8 text string (markdown, JSON, plain text). |
| `transition_stage` | `tools/write.py` | `transition_stage(ticket_id: int, stage_name: str, model: str = 'project.task') → dict` | Move a task to a stage by name. Raises if the name is not found or ambiguous. |
| `list_metadata` | `tools/utils.py` | `list_metadata(resource: str, project_id: int | None = None, fresh: bool = False) → list[dict]` | List Odoo metadata. resource: projects | stages | users | tags. project_id filters stages. |
| `add_project_to_graph` | `tools/graph_admin.py` | `add_project_to_graph(project_id: int) → dict` | Add a project to the active graph by hydrating it from Odoo. Idempotent — calling twice re-hydrates. Returns {project_id, name, ticket_count, hydration_ms}. |
| `remove_project_from_graph` | `tools/graph_admin.py` | `remove_project_from_graph(project_id: int) → dict` | Remove a project from the active graph and free memory. Subsequent reads of this project fall through to RPC. Returns {project_id, removed}. |
| `list_active_projects` | `tools/graph_admin.py` | `list_active_projects() → list` | List all projects currently in the active graph. Returns [{project_id, name, ticket_count, last_synced_at_iso}]. |
| `refresh_project_graph` | `tools/graph_admin.py` | `refresh_project_graph(project_id: int) → dict` | Delta sync a project already in the active graph. Queries Odoo for tickets changed since last hydration, patches graph, detects deletions. Returns {project_id, updated_count, removed_count}. |
| `view_graph` | `tools/graph_admin.py` | `view_graph(project_id: int, format: str = 'tree') → object` | Render the current graph snapshot for a project. |
